<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Document</title>
    <style>
        * {
      margin: 0;
      padding: 0;
      box-sizing: border-box;
    }

    body, html {
      height: 100%;
      font-family: Arial, sans-serif;
    }

    /* Background */
    .background {
      position: fixed;
      top: 0;
      left: 0;
      width: 100%;
      height: 100%;
      background: url("https://images.pexels.com/photos/268533/pexels-photo-268533.jpeg") no-repeat center center/cover;
      filter: blur(8px);      /* background blur */
      z-index: -1;            /* मागे पाठवले */
    }

    /* Center form */
    .login-container {
      height: 100vh;
      display: flex;
      justify-content: center;
      align-items: center;
    }

    /* Login box */
    .login-box {
      width: 350px;
      padding: 30px;
      background: rgba(255, 255, 255, 0.15);   /* lightly transparent */
      border-radius: 15px;
      box-shadow: 0 0 15px rgba(0,0,0,0.3);
      backdrop-filter: blur(6px);              /* glassmorphism effect */
      -webkit-backdrop-filter: blur(6px);
      color: #fff;
    }

    .login-box h2 {
      text-align: center;
      margin-bottom: 20px;
      color: #fff;
    }

    .login-box input {
      width: 100%;
      padding: 12px;
      margin: 10px 0;
      border: none;
      outline: none;
      border-radius: 8px;
    }

    .login-box button {
      width: 100%;
      padding: 12px;
      margin-top: 15px;
      border: none;
      border-radius: 8px;
      background: #ff9800;
      color: white;
      font-size: 16px;
      font-weight: bold;
      cursor: pointer;
      transition: 0.3s;
    }

    .login-box button:hover {
      background: #e68900;
    }
    @media screen and (max-width: 480px) {
        .login-box {
            padding: 20px;
        }
        .login-box h2 {
            font-size: 1.5em;
        }
        .login-box input {
            padding: 10px;
            font-size: 0.9em;
        }
        .login-box button {
            padding: 10px;
            font-size: 0.9em;
        }
        }
    </style>
</head>
<body>
<div class="background"></div>    
 <div class="login-container">
   <div class="login-box">
    <form method="post" action="/api/register">
        <input type="text" name="fullname" placeholder="Enter your name" />
        <br/>
        <br/>
        <input type="text" name="email" placeholder="Enter email" />
        <br/>
        <br/>
        <input type="password" name="password_hash" placeholder="Enter password" />
        <br/>
        <br/>
        <button>Submit</button>
    </form>
</div>
</div>
</body>
</html>